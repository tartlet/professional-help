import { useState } from "react";

export default MenuBox = () => {
    [isVisible, setIsVisible] = useState(false);

    toggleMenu = () => {
        setIsVisible(true);
    }
    return (

    );
};